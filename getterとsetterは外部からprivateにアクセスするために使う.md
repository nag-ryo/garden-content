タイトルの通り。

基本的にはライブラリを作るときなどに使う機会が多いらしい。
privateプロパティを操作出来る時点でオブジェクトのカプセル化は守られていない。
基本的には避ける実装だと思われる。

下記の実装とかは完全に意味ない。
```ts
private static get state() {
	return Test._state;
}
```
知らないときにやってしまったのだが、本当に意味がわからない実装だと思う。
