## 失敗例
Browserクラスを作ったとき、ブラウザ起動処理に時間がかかるので、先に非同期の起動メソッドを作って起動しておくようにした。
しかし、起動メソッドの役割はリトライも含めて起動し切ること。
同期してでも起動が保証されるべきである。

## 対応
マイクロサービスのような呼び出し元の後続処理が無い場合はいいが、そうでなければ基本的には必ず同期させる。
非同期処理で投げっぱなしで後で同期するパターンは考えないようにする。